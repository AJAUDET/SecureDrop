version: "3.9"

services:
  host:
    container_name: host
    build:
      context: .
      dockerfile: Dockerfile
    command: python3 securedrop.py
    ports:
      - "8080:80"
    volumes:
      - .:/app               # lets you edit files live
    stdin_open: true         # allows attaching an interactive shell
    tty: true                # keeps a TTY open
    restart: always
    networks:
      - securedrop_securenet

  receiver:
    container_name: receiver
    build:
      context: .
      dockerfile: Dockerfile
    command: python3 securedrop.py
    ports:
      - "8081:80"
    volumes:
      - .:/app
    stdin_open: true
    tty: true
    restart: always
    networks:
      - securedrop_securenet

networks:
  securedrop_securenet:
    ipam:
      driver: default
      config:
        - subnet: 192.168.123.0/24
